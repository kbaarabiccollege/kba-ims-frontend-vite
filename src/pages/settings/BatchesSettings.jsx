// src/pages/settings/BatchesSettings.jsx
//
// Placeholder — if a Batches page/feature already exists elsewhere in
// the app (e.g. under pages/admin/academics/), point the /settings/batches
// route at that component instead of this one. Share the file and I'll
// wire it in directly.

const BatchesSettings = () => {
    return (
      <div className="settings-subpage">
        <div className="settings-subpage-header">
          <h2>Batches</h2>
          <p>Create and manage batches/sections used across attendance, timetable, and examinations.</p>
        </div>
        <div className="settings-subpage-placeholder">
          Batches management UI goes here.
        </div>
      </div>
    );
  };
  
  export default BatchesSettings;